- [Alt-data tiles reuse](alt-data-tiles.md) — shared Apify alt-data cards are bound to ONE focus ticker per page (not per-row) to avoid fanning out an Apify run per visible row.
- [e2e cold-compile flakiness](e2e-cold-compile-flakiness.md) — platform Playwright flakes (ERR_ABORTED) on heavy Next dev routes; fix = gotoWithRetry + prewarmHeavyRoutes + low retries, all three.
- [e2e validation parallelism](e2e-validation-parallelism.md) — validation runs e2e concurrently with build/lint/typecheck/test, starving the dev server; smoke flakes are environmental — fix with `retries: 1` + nav retry, never weaken specs.
- [E2E smoke name matching](e2e-smoke-name-matching.md) — tests/platform specs match by accessible name/visible copy; any UI rename must update the spec regex or it times out looking like a product bug.
- [Quote provider waterfall & dead-key handling](quote-provider-waterfall.md) — /api/quote degrades across providers; never add a rejected-key circuit-breaker (breaks credential-health recovery detection).
- [Internal loopback fetch anti-pattern](internal-loopback-fetch-antipattern.md) — server routes must call lib fns (e.g. dailyBarsWaterfall) directly, not fetch sibling /api routes over loopback (flaky under concurrency).
- [agent-tool-result-payload shape locked](agent-tool-result-payload-shape.md) — extra per-result fields (e.g. citeIndex) go on the SSE frame wrapper, NOT in buildAgentToolResultPayload (exact-keyset test).
- [Supabase migration](supabase-migration.md) — runtime-managed DATABASE_URL quirks, drizzle-kit push interactive prompt on Supabase, DATABASE_MIGRATION_URL convention.
- [Playwright click vs evaluate for React](playwright-react-click.md) — use chip.evaluate(el => el.click()) not click({ force: true }) when a positioned overlay intercepts; also use it for close-button clicks under toast overlays.
- [Always-in-DOM Drawer visibility](drawer-visibility.md) — Drawer in ui/index.tsx is always in the DOM (CSS transform only); toBeVisible() is always true; toBeHidden() always false. Assert on content, not the aside element.
- [Screener Drawer two-element strict mode](screener-drawer-strict.md) — company page renders "Recent decks" Drawer always-in-DOM alongside the citation Drawer; filter aside[role="dialog"] by hasText to avoid strict-mode violations.
- [Agent tools calling protected internal routes](agent-tool-internal-fetch-auth.md) — protecting an /api/* route an agent tool calls server-side requires forwarding `fwd` auth in that tool, or it 401s in prod (works in OPEN_MODE demo, hiding the break).
- [drizzle-kit in a Next server bundle](drizzle-kit-next-bundling.md) — drizzle-kit/api breaks next build; serverExternalPackages & webpackIgnore (stripped by SWC) don't work — externalize via next.config webpack rule.
- [node:test + tsx top-level await](node-test-tsx-top-level-await.md) — unit tests run under tsx cjs transform; await import() of the module under test must go in before(), not top-level; run a single file from the package dir.
- [Optional @workspace/db imports](optional-db-modules.md) — @workspace/db throws at import without DATABASE_URL; lazy dynamic-import + catch to keep a module DB-optional.
- [Polymarket & Kalshi public API quirks](prediction-markets-apis.md) — Polymarket /markets caps at 100 (use public-search); Kalshi uses _dollars/_fp fields & is only browsable per-series, not via /markets.
- [GDELT geopolitical events provider](gdelt-geopolitical-events.md) — keyless DOC 2.0 API rate-limits ~1 req/5s (429 + plain-text, not JSON); severity is category-derived not a forecast; pass ISO-2 country (provider maps to FIPS).
- [Marketing SPA prerender + clean URLs](marketing-prerender-clean-urls.md) — ship per-route SEO/JSON-LD in initial HTML via build-time prerender; emit FLAT `<route>.html` (not dir index) so static `/product` resolves, not the SPA fallback.
- [artifacts/platform not typechecked by CI](platform-not-typechecked.md) — no typecheck script + ignoreBuildErrors:true means green CI ≠ clean platform TS; run bare tsc -p, filter to your files only.
