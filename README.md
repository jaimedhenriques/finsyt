# finsyt

## Agent skills

This repository is configured to use `addyosmani/agent-skills` as an engineering workflow baseline.

Install/update and sync rules:

```bash
./scripts/install-agent-skills.sh
./scripts/sync-cursor-skills.sh
```

See `docs/agent-skills.md` for details.